import { InputProps } from './Input';

const base: InputProps = {
  type: 'text',
};

const alternateBase: InputProps = {
  type: 'text',
};

export const mockInputProps = {
  base,
  alternateBase,
};
